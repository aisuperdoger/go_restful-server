syntax = "proto3";
package utils;
option go_package = "./utils";


message Image {
  bytes image = 1; // 需要注意的是，图片数据使用 bytes 类型进行传输。在返回图片数据时，通常需要将其转换为 Base64 编码后再传输。
}
message  VideoPath{
  string path = 1;
}